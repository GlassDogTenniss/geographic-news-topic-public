# Topic map place mapping review - batch 130

Batch 130 continues the unresolved `game-all` place mapping pass.

## Added data file

Added:

- `data/unresolved_known_places_143.json`

## Scope

This batch adds one low-priority region-level mapping for a residual cultural representation article.

The mapping is title-specific and does not add a broad regional fallback.

## Notes

A separate residual setting article was reviewed but not added because the write was blocked by safety checks. Other remaining unresolved items are mostly list articles, broad commentary, or titles without enough stable place signal.
